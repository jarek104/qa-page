export interface Ilink {
    linkID: number;
    linkName: string;
    linkURL: string;
    linkDescription: string;
    linkColor: string;
    linkClickCounter: number;
    // This should have 'Copy URL' button
}
